# 청킹 파라미터
# 한글 문서는 영문 대비 정보 밀도가 높아 chunk_size를 약간 작게 설정

# 청크 최대 문자 수
chunk_size: 800

# 청크 간 중복 문자 수 (문맥 연속성 보장)
chunk_overlap: 150

# 청킹 전략: "recursive" = RecursiveCharacterTextSplitter
strategy: "recursive"
